/**
 * @class IAmountRanges
 * @abstract
 * Amount Ranges properties from Rate Program
 * Amount Ranges `Interface`
 * @property {number} min
 * @property {number} max
 * @version 1.0.0
 */
export interface IAmountRanges {
	min: number;
	max: number;
}
