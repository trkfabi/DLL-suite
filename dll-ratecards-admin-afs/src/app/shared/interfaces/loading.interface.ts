/**
 * @class LoadingStatus
 * @abstract
 * Determines if the app is busy or not and display the `LoadingComponent`
 * LoadingStatus `Interface` Data
 * @version 1.0.0
 */
export interface ILoadingStatus {
	busy: boolean;
}
