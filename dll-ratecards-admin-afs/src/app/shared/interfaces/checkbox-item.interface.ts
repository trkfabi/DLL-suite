/**
 * @class ICheckboxItem
 * @abstract
 * Properties available for a Checkbox Item
 * Checkbox Item `Interface`
 * @property {String} name
 * @property {String} value
 * @property {Boolean} checked
 * @version 1.0.0
 */
export interface ICheckboxItem {
	name: string;
	value: string;
	checked: boolean;
}
